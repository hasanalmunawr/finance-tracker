spring:
  datasource:
    url: ${MARIADB_URL:jdbc:mariadb://8.215.195.63:3306/finance_tracker}
    username: ${MARIADB_USER:badiro}
    password: ${MARIADB_PASSWORD:badiro07}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
  mail:
    host: smtp.gmail.com
    port: 465
    username: hasanalmunawar.it@gmail.com
    password: wdktgjojfmwoydks
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
      transport:
        protocol: smtp
    default-encoding: UTF-8

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days --> not used
  mailing:
    frontend:
      activation-url: https://fintrack.hasanalmunawar.my.id
server:
  port: 8088